spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/security-demo?useUnicode=yes&characterEncoding=utf-8
    username: root
    password: root
  session:
    store-type: none

security:
  basic:
  # 打开/关闭spring security 打开默认的用户名为user，密码是程序启动时输出的
    enabled: false

server:
  port: 9090

gwf:
  security:
    browser:
      loginPage: /demo-signIn.html
      loginType: JSON
#    code:
#      image:
#        width: 100  宽高可从浏览器获取
#        heigth: 100
#        length: 6
#        url: /user,/user/*
#        expireIn: 160
#      sms:
#        length: 6
#        url: /user,/user/*
#        expireIn: 160

